int main() { int x = 10; int *ptr = &x; // Assign the address of x to ptr *ptr = 20; // Modify the value at the address stored in ptr printf("%d\n", x); // Output: 20\n return 0; } int foo(int *x) { // x is a pointer to an integer *x = 30; // Modify the value at the address pointed to by x return 0; } int main2() { int x = 10; foo(&x); // Pass the address of x to foo printf("%d\n", x); // Output: 30\n return 0; } 